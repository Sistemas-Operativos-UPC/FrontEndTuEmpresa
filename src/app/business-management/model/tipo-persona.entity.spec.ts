import { TipoPersonaEntity } from './tipo-persona.entity';

describe('TipoPersonaEntity', () => {
  it('should create an instance', () => {
    expect(new TipoPersonaEntity()).toBeTruthy();
  });
});
