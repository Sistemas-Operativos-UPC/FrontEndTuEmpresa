import { TipoDocumentoEntity } from './tipo-documento.entity';

describe('TipoDocumentoEntity', () => {
  it('should create an instance', () => {
    expect(new TipoDocumentoEntity()).toBeTruthy();
  });
});
