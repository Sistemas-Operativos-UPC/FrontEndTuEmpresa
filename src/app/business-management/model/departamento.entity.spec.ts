import { DepartamentoEntity } from './departamento.entity';

describe('DepartamentoEntity', () => {
  it('should create an instance', () => {
    expect(new DepartamentoEntity()).toBeTruthy();
  });
});
